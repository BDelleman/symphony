# Symphony .worktreeinclude example
# Copy only gitignored artifacts that speed up local builds.
# Paths are relative to the source repository root.
# Keep dotenv and credential files in the primary worktree only.
